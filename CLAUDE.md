# 작업 지침

- 요청받은 작업은 한 번에 완전무결하게 구현한다.
- 검증(테스트 실행, lint/build 확인, 브라우저 스크린샷 확인 등)은 하지 않는다.
- 중간 산출물, 실행/테스트 과정에서 생긴 파일, 그리고 기능과 상관없는 파일·폴더는
  직접 삭제하지 않는다. 프로젝트 루트에 `삭제/` 폴더를 만들어 전부 그 안에 옮겨
  넣는다 — 실제 삭제는 사용자가 직접 한다.
